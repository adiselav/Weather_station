# ============================================
# ESP32C3 WEATHER STATION DASHBOARD CONFIGURATION
# ============================================
# Rename this file to .env and fill in the values

# MongoDB Atlas Connection String
# Get it from MongoDB Atlas: Database -> Connect -> Connect your application
# Replace YOUR_USERNAME, YOUR_PASSWORD, YOUR_CLUSTER, and YOUR_DATABASE with your actual credentials
MONGODB_URL="mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/YOUR_DATABASE?retryWrites=true&w=majority"

# Database name
DB_NAME="weather_station"

# Collection name
COLLECTION_NAME="readings"

# Port (automatically injected on Render/Railway, local uses 4000)
PORT=4000

# Auto-refresh interval in seconds (REQUIRED - no default value)
REFRESH_INTERVAL_SECONDS=30
